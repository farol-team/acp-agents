[package]
name = "acp-agents"
description = "Which coding agents speak the Agent Client Protocol, and where to find them on a machine"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]

[dev-dependencies]
tempfile = "3"
